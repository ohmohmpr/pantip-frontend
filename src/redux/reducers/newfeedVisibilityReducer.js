import { SHOW_PANTIP_NOW, SHOW_PANTIP_PICK, SHOW_PANTIP_TREND, SHOW_MY_FEED } from "../constants/action-types";


function newfeedVisibilityReducer (visibility = SHOW_PANTIP_NOW, action) {
    switch(action.type) {
        case
    }
}